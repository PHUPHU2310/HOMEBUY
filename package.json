{
  "name": "homebuy",
  "version": "1.0.0",
  "description": "Real Estate Platform - Full Stack",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "landing"
  ],
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "dev:all": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" \"cd landing && npm start\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../landing && npm install",
    "build": "cd backend && npm run build",
    "build:all": "cd backend && npm run build && cd ../frontend && npm run build && cd ../landing && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
